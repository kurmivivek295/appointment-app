spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config-server
  profiles:
    include: native
#        git:
#          uri: https://github.com/kurmivivek295/spring-properties.git
#          username:
#          password:
#          searchPaths: "*"
  security:
    basic:
      enabled: true
    user:
      name: config123
      password: config123