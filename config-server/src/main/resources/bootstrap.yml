spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: <Git-Uri-To-Repo>
          username: <Git-Username>
          password: <Git-Password>
          searchPaths: "*"
        health:
          enabled: false
  security:
    basic:
      enabled: true
    user:
      name: config_client
      password: client123